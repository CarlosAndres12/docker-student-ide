Describe "R-02 runtime persistence" -Tag ExternalVM {
    It "restores a baseline and verifies a marker across stack restart" -Pending {
        # Implement after snapshot and SSH adapters exist under scripts/windows-testing.
    }
}
