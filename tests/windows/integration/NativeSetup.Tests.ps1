Describe "N-10 native toolchain provision" -Tag NativeWindows {
    It "installs Git, Node, Python, and VS Code on a disposable Windows fixture" -Pending {
        # Implement against a disposable Windows 11 fixture (no Docker).
    }
}

Describe "N-11 native runtime and agent verification" -Tag NativeWindows {
    It "verifies the Python stack, AI agents, and VS Code extensions" -Pending {
        # Implement against a disposable Windows 11 fixture (no Docker).
    }
}
