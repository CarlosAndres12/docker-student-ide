Describe "I-04 restricted execution policy" -Tag NativeWindows {
    It "runs the staged installer under a disposable restricted-policy fixture" -Pending {
        # Implement after the native Windows fixture and policy restore adapter exist.
    }
}
