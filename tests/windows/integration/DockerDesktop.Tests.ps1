Describe "S-02 Docker Desktop readiness" -Tag NativeWindows {
    It "starts Docker Desktop and waits for docker info" -Pending {
        # Implement against the prepared-runtime native Windows baseline.
    }
}

Describe "R-01 prepared runtime" -Tag NativeWindows {
    It "starts the Compose runtime and serves the browser endpoint" -Pending {
        # Implement against the prepared-runtime native Windows baseline.
    }
}
