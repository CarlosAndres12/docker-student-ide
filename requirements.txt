# =============================================================================
# Python ML/DL Stack — ALL packages pinned to exact versions for reproducibility
# =============================================================================
# CPU-only by default. See GPU variant comments at the bottom.
#
# PyTorch CPU wheels are installed via a separate pip step in the Dockerfile
# using --index-url https://download.pytorch.org/whl/cpu
# =============================================================================

# ── Core Data Science ────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2

# ── Deep Learning (CPU-only defaults) ────────────────────────────────────────
# PyTorch and torchvision installed separately via CPU wheel index
torch==2.4.1
torchvision==0.19.1
# CPU-only TensorFlow
tensorflow-cpu==2.17.0

# ── Experiment Tracking & Hyperparameter Optimisation ────────────────────────
optuna==4.0.0
mlflow==2.17.0

# ── Gradient Boosting ────────────────────────────────────────────────────────
xgboost==2.1.1
lightgbm==4.5.0

# ── NLP & Computer Vision ────────────────────────────────────────────────────
transformers==4.44.2
opencv-python==4.10.0.84
nltk==3.9.1

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# ── Notebooks ────────────────────────────────────────────────────────────────
jupyter==1.1.1
jupyterlab==4.2.5
ipywidgets==8.1.5

# ── Developer Quality-of-Life ────────────────────────────────────────────────
black==24.8.0
ruff==0.6.9
pytest==8.3.3
python-dotenv==1.0.1

# =============================================================================
# GPU VARIANT  (commented out by default)
# =============================================================================
# To enable GPU support:
#   1. Uncomment the lines below in requirements.txt
#   2. In the Dockerfile, change PyTorch index-url to
#      https://download.pytorch.org/whl/cu121
#   3. Use the full tensorflow package (not tensorflow-cpu)
#   4. Install the NVIDIA Container Toolkit on the host
#      (https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/)
#   5. Set DEVICE=gpu in .env (activates deploy.resources in docker-compose.yml)
#
# torch==2.4.1              # GPU: use https://download.pytorch.org/whl/cu121
# torchvision==0.19.1       # GPU: use https://download.pytorch.org/whl/cu121
# tensorflow==2.17.0        # GPU: full tensorflow (not -cpu) with NVIDIA libs
# =============================================================================
